# =================================
# Production Environment Variables
# =================================

# Domain Configuration (REQUIRED for SSL)
# Ganti dengan domain Anda, contoh: scandata.example.com
DOMAIN=scandata.yourdomain.com

# MySQL Configuration
MYSQL_ROOT_PASSWORD=your-secure-root-password
MYSQL_DATABASE=scandata
MYSQL_USER=scandata
MYSQL_PASSWORD=your-secure-db-password

# JWT Configuration
# PENTING: Ganti dengan secret key yang kuat untuk production!
JWT_SECRET=generate-a-very-long-random-string-here-min-32-chars
JWT_EXPIRY_HOURS=24

# CORS Configuration
# Akan otomatis menggunakan DOMAIN dari atas
ALLOWED_ORIGINS=https://scandata.yourdomain.com
